# ./.unity-tests.yml

name: unity-tests
root: ~/sandboxes/unity/

windows:
  - unity-tests:
      layout: even-horizontal
      panes:
        - (cd landscape/components/digital_banking/tests && source runtime-env && cd ~/sandboxes/unity && run component-test-check)
        - run api-stubs

  - client:
      layout: main-horizontal
      panes:
        - run client
