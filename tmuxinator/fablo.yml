# ./.tmuxinator.yml

name: fablo
root: ~/sandboxes/fablo/

windows:
  - fablo:
      layout: 18fb,189x45,0,0[189x33,0,0{52x33,0,0,21,136x33,53,0,37},189x11,0,34,32]

      # Synchronize all panes of this window, can be enabled before or after the pane commands run.
      # "before" represents legacy functionality and will be deprecated in a future release, in favour of "after"
      # synchronize: after
      panes:
        - echo "Running fablo"
        - echo "Loading tmuxinator from local"
               #        - cd backend && docker build . && docker-compose up -d --build && docker-compose logs -ft
        - echo "Running..."

  - db: psql

